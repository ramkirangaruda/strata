# build output
/crashwriter
*.test

# scratch databases
/tmp/
*.sst
*.log
MANIFEST-*
CURRENT

# accidental build artifact, cleaned up
strata.tar.gz
strata-repo.tar.gz
*.tar.gz

# demo server
/server
/bin/
/data/
.env

# local docker volumes
/.docker-data/
# Playwright CLI output (may contain credentials)
.playwright-cli/
